// Investigated failed authentication events for the lab account
SigninLogs
| where UserPrincipalName == "michaelsmith@labtest.gmail.onmicrosoft.com"
| where ResultType != 0
| project TimeGenerated, UserPrincipalName, IPAddress, ResultType, ResultDescription, AppDisplayName, Location, DeviceDetail
| order by TimeGenerated desc
